# =============================================================================
# 📦 Facebook Outreach & Server Dependencies (Render Production)
# =============================================================================

# FastAPI & Uvicorn Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# PostgreSQL Database driver
psycopg2-binary>=2.9.9

# Redis Client for atomic quota tracking
redis>=5.0.1

# DNS Resolver for zero-send email validation (MX record check)
dnspython>=2.6.1

# Environment variable loader (.env & .env.local)
python-dotenv>=1.0.1

# HTTP requests library
requests>=2.31.0
